d() {
	?? $# = 0 && cd /home/code && return
	ff d ${1/\//*/} /home/code && cd $match
}

dv() {
	?? $# = 2 && d $2
	$1
}

dV() {
	dv $@
	shh cd -
}

alias ds='dv start'
alias db='dv build'
alias dbd='dv build-data'
