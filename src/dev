alias d='dw'
export DW=/home/code/web

dw() {
	?? $# = 0 && cd $DW && r0
	ff d ${1/\//*/} $DW && cd $match
}

ds() {
	ff d ${1/\//*/} $DW && dw || r1
	NAME=${match##*/} \
		vite $DW ${@:2}
}

dv() {
	vite $@
}

alias dbd='build-data'
