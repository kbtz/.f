d() {
	[ $# -ne 1 ] && cd /home/code && return
	ff d $1 /home/code && cd $match
}

alias db='build'
alias dbd='build-data'

di() {
	cwd=`npm prefix`/lib
	mkdir -p $cwd
	ni -g --prefix $cwd
}

dd() {
	? $# = 1 && d $1
	vite
}
