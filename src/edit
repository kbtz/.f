alias e='nvim'

er() {
	e $(realpath $1)
}

ee() {
	er `first $1`
}

ecd() {
	[ $# -eq 2 ] || { cd $1; cr;	return 0; }
	
	ff $1 $2				\
		&& er $match	\
		|| cd $1
	unset match
}

alias es='ecd $_DOT/src'
alias ec='ecd $_DOT/cfg'
alias eC='ecd ~/.config'
