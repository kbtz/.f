alias e='nvim'

er() {
	e $(realpath $1)
}

ee() {
	er `first $1`
}

eff() {
	[ $# -eq 1 ] && { cd $1; cr; return 0; }
	
	ff $1 $2				\
		&& er $match	\
		|| cd $1
	
	unset match
}

alias es='eff $_DOT/src'
alias ec='eff $_DOT/cfg'
alias eC='eff ~/.config'
